import * as React from 'react';
import { ReactFunction } from './utils';
declare const useTouch: (element: JSX.Element | ReactFunction) => [React.ReactElement<any>, boolean];
export default useTouch;
